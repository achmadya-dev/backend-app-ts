paths:
    /api/external:
        get:
            summary: Get external APIs
            tags: [External]
            security:
                - BearerAuth: []
            responses:
                200:
                    description: External APIs fetched successfully
                401:
                    description: Unauthorized
                500:
                    description: Internal server error
